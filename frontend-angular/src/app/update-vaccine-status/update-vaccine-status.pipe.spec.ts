import { UpdateVaccineStatusPipe } from './update-vaccine-status.pipe';

describe('ViewUsersPipe', () => {
  it('create an instance', () => {
    const pipe = new UpdateVaccineStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
