import { ViewUsersPipe } from './view-users.pipe';

describe('ViewUsersPipe', () => {
  it('create an instance', () => {
    const pipe = new ViewUsersPipe();
    expect(pipe).toBeTruthy();
  });
});
